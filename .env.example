#
# CASSANDRA DATABASE Config
#
# DB_USERNAME - default. has to be changed to local one username if it is different
# DB_PASSWORD - default. has to be changed to local one password if it is different
# DB_HOST - default. has to be changed to local used host if it is different
# DB_PORT - default. has to be changed to local used port if it is different
# DB_AUTH_PROVIDER - default. has to be changed to local used provider if it is different
# DB_LOCAL_DATA_CENTER  - default. has to be changed to local used data center if it is different
DB_USERNAME=cassandra
DB_PASSWORD=cassandra
DB_HOST=127.0.0.1
DB_PORT=9042
DB_AUTH_PROVIDER=PlainText
DB_LOCAL_DATA_CENTER=datacenter1
#
# SAVE JSON SCHEMA INTO FILE BY PATH
#
JSON_SCHEMA_SAVED_FILE=