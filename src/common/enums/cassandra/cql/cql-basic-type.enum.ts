enum CQLBasicDataType {
  ASCII = 'ascii',
  BIGINT = 'bigint',
  BOOLEAN = 'boolean',
  COUNTER = 'counter',
  DATE = 'date',
  DECIMAL = 'decimal',
  DOUBLE = 'double',
  FLOAT = 'float',
  INET = 'inet',
  INT = 'int',
  SMALLINT = 'smallint',
  TEXT = 'text',
  TIME = 'time',
  TIMESTAMP = 'timestamp',
  TIMEUUID = 'timeuuid',
  TINYINT = 'tinyint',
  UUID = 'uuid',
  VARCHAR = 'varchar',
  VARINT = 'varint',
}

export { CQLBasicDataType };
